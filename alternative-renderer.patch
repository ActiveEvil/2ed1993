diff --git a/app/factions/[faction]/[list]/page.tsx b/app/factions/[faction]/[list]/page.tsx
index 0b9d597..57c3070 100644
--- a/app/factions/[faction]/[list]/page.tsx
+++ b/app/factions/[faction]/[list]/page.tsx
@@ -8,6 +8,8 @@ import {
   CharacteristicRow,
   CharacteristicTable,
   ProfileFrame,
+  branchesOf,
+  inBranch,
 } from "@/components/CharacteristicProfile";
 import { SectionHeading } from "@/components/Heading";
 import { Highlighter, HighlighterLink } from "@/components/Highlighter";
@@ -37,7 +39,7 @@ const loadList = (faction: string, list: string) =>
   supabase
     .from("army_lists")
     .select(
-      "id, name, description, factions!inner(slug, name), unit_categories(category, note, min_percent, max_percent, position, army_list_allowance_rules!army_list_allowance_rules_unit_category_id_fkey(id, count, per_count, note, per_entry:army_list_entries!army_list_allowance_rules_per_entry_id_fkey(units(name))), army_list_entries(id, position, allowance_min, allowance_max, points, note, transport_scope, entry_group:army_list_entry_groups(id, name, position), points_bases(name), army_list_entry_options(id, position, points, points_percent, note, unit_profile_id, unit_option_id, points_bases(name), weapons!army_list_entry_options_weapon_id_fkey(name)), army_list_allowance_rules!army_list_allowance_rules_army_list_entry_id_fkey(id, count, per_count, note, per_entry:army_list_entries!army_list_allowance_rules_per_entry_id_fkey(units(name))), units(id, name, factions(slug), datafaxes(id, points), unit_profiles(id, name, position, alternative, models_min, models_max, mastery_level, wargear_cards_max, m, ws, bs, s, t, w, i, a, ld, unit_profile_weapons(id, quantity, alternative, position, weapons(name)), unit_profile_armour(armour_id, position, alternative, save_override, armour(name)), unit_profile_wargear_cards(position, card:wargear_cards(name))), unit_options!unit_options_unit_id_fkey(id, models_min, models_max, models_per, whole_unit, quantity, grant_mode, restriction, note, option_group, position, profile:unit_profiles!unit_options_unit_profile_id_fkey(name), upgrade:unit_profiles!unit_options_to_unit_profile_id_fkey(name, units(name)), replaces:weapons!unit_options_replaces_weapon_id_fkey(name), grants:weapons!unit_options_weapon_id_fkey(name), grants_armour:armour!unit_options_armour_id_fkey(name), replaces_armour:armour!unit_options_replaces_armour_id_fkey(name), card:wargear_cards(name), unit_option_categories(position, wargear_categories(category))), unit_special_rule_assignments(position, note, rule:unit_special_rules(id, name, rule, rule_id, anchor, rules(id, name, rule_categories(slug))))))), army_list_allies!army_list_allies_army_list_id_fkey(id, position, note, factions(slug, name), ally_list:army_lists!army_list_allies_ally_army_list_id_fkey(slug, name, factions(slug, name))), wargear_categories(category, note, wargear_items(id, points, restriction, armour(name), weapons(name), units(name, factions(slug), datafaxes(points))))",
+      "id, name, description, factions!inner(slug, name), unit_categories(category, note, min_percent, max_percent, position, army_list_allowance_rules!army_list_allowance_rules_unit_category_id_fkey(id, base_count, count, per_count, note, per_entry:army_list_entries!army_list_allowance_rules_per_entry_id_fkey(units(name))), army_list_entries(id, position, allowance_min, allowance_max, points, note, transport_scope, counts_as_entry_id, models_max_army, entry_group:army_list_entry_groups(id, name, position), points_bases(name), army_list_entry_options(id, position, points, points_percent, note, unit_profile_id, unit_option_id, weapon_profile_id, points_bases(name), weapons!army_list_entry_options_weapon_id_fkey(name, weapon_profiles(id, name))), army_list_allowance_rules!army_list_allowance_rules_army_list_entry_id_fkey(id, base_count, count, per_count, note, per_entry:army_list_entries!army_list_allowance_rules_per_entry_id_fkey(units(name))), units(id, name, factions(slug), datafaxes(id, points), unit_profiles(id, name, position, alternative, models_min, models_max, mastery_level, wargear_cards_max, m, ws, bs, s, t, w, i, a, ld, unit_profile_weapons(id, quantity, alternative, position, weapons(name)), unit_profile_armour(armour_id, position, alternative, save_override, armour(name)), unit_profile_wargear_cards(position, card:wargear_cards(name))), unit_options!unit_options_unit_id_fkey(id, models_min, models_max, models_per, whole_unit, quantity, grant_mode, restriction, note, option_group, position, weapon_profile_id, profile:unit_profiles!unit_options_unit_profile_id_fkey(name), upgrade:unit_profiles!unit_options_to_unit_profile_id_fkey(name, units(name)), replaces:weapons!unit_options_replaces_weapon_id_fkey(name), granted:units!unit_options_granted_unit_id_fkey(name), grants:weapons!unit_options_weapon_id_fkey(name, weapon_profiles(id, name)), grants_armour:armour!unit_options_armour_id_fkey(name), replaces_armour:armour!unit_options_replaces_armour_id_fkey(name), card:wargear_cards(name), unit_option_categories(position, wargear_categories(category))), unit_special_rule_assignments(position, note, unit_profile_id, rule:unit_special_rules(id, name, rule, rule_id, anchor, rules(id, name, rule_categories(slug))))))), army_list_allies!army_list_allies_army_list_id_fkey(id, position, note, factions(slug, name), ally_list:army_lists!army_list_allies_ally_army_list_id_fkey(slug, name, factions(slug, name))), wargear_categories(category, note, wargear_items(id, points, restriction, armour(name), weapons(name), units(name, factions(slug), datafaxes(points))))",
     )
     .eq("slug", list)
     .eq("factions.slug", faction)
@@ -92,6 +94,8 @@ type Entry = {
   wargearCardsMax: number | null;
   note: string | null;
   transport: string | null;
+  countsAs: string | null;
+  armyCap: string | null;
   rules: string[];
   extras: string[];
   rows: CharacteristicRow[];
@@ -139,6 +143,9 @@ const TRANSPORT_SCOPES: Readonly<Record<string, string>> = {
 const transportLine = (scope: string | null): string | null =>
   scope === null ? null : (TRANSPORT_SCOPES[scope] ?? null);
 
+const takenAs = (name: string): string =>
+  `Taken as ${/^[aeiou]/i.test(name) ? "an" : "a"} ${name}.`;
+
 const range = (min: number, max: number | null): string =>
   max === null || max === min ? String(min) : `${min}–${max}`;
 
@@ -164,6 +171,7 @@ const profileNotes = (profile: {
 };
 
 const allowanceRule = (rule: {
+  base_count: number | null;
   count: number;
   per_count: number;
   note: string | null;
@@ -174,7 +182,16 @@ const allowanceRule = (rule: {
   }
 
   if (rule.per_count === 1 && rule.per_entry) {
-    return `Up to ${rule.count} per ${rule.per_entry.units.name}`;
+    const per = rule.per_entry.units.name;
+
+    if (rule.base_count === null) {
+      return `Up to ${rule.count} per ${per}`;
+    }
+
+    const base = rule.base_count === 1 ? "One" : String(rule.base_count);
+    const more = rule.count === 1 ? "one more" : `${rule.count} more`;
+
+    return `${base}, and ${more} for each ${per}`;
   }
 
   return null;
@@ -184,6 +201,7 @@ const buildEntry = (
   entry: RawEntry,
   category: string,
   listFactionSlug: string,
+  unitByEntryId: ReadonlyMap<number, string>,
 ): Entry => {
   const basis = entry.points_bases?.name ?? null;
   const options = entry.army_list_entry_options;
@@ -213,10 +231,12 @@ const buildEntry = (
     }
   }
 
+  const branches = branchesOf(profiles);
+  const grades = profiles.filter((profile) => inBranch(profile, branches));
   const graded =
     entry.points === null &&
-    profiles.length > 1 &&
-    profiles.every(({ id }) => gradeByProfile.has(id));
+    branches.size > 1 &&
+    grades.every(({ id }) => gradeByProfile.has(id));
 
   const wargearCardsMax =
     profiles.length > 0 &&
@@ -229,6 +249,10 @@ const buildEntry = (
 
   const datafax = entry.units.datafaxes;
   const factionSlug = entry.units.factions?.slug ?? listFactionSlug;
+  const countsAs =
+    entry.counts_as_entry_id === null
+      ? null
+      : (unitByEntryId.get(entry.counts_as_entry_id) ?? null);
 
   let cost: string | null = null;
 
@@ -316,6 +340,11 @@ const buildEntry = (
     wargearCardsMax,
     note: entry.note,
     transport: transportLine(entry.transport_scope),
+    countsAs: countsAs === null ? null : takenAs(countsAs),
+    armyCap:
+      entry.models_max_army === null
+        ? null
+        : `No more than ${entry.models_max_army} models in the army.`,
     rules: entry.army_list_allowance_rules
       .map(allowanceRule)
       .filter((text): text is string => text !== null),
@@ -333,8 +362,18 @@ const buildEntry = (
 
             if (option.weapons) {
               const cost = formatOptionCost(option);
-
-              return `${option.weapons.name}${cost === null ? "" : `, ${cost}`}`;
+              const profile =
+                option.weapon_profile_id === null
+                  ? null
+                  : (option.weapons.weapon_profiles.find(
+                      ({ id }) => id === option.weapon_profile_id,
+                    )?.name ?? null);
+              const name =
+                profile === null
+                  ? option.weapons.name
+                  : `${option.weapons.name} (${profile})`;
+
+              return `${name}${cost === null ? "" : `, ${cost}`}`;
             }
 
             return null;
@@ -421,6 +460,8 @@ const Details: React.FC<{ entry: Entry; showNote: boolean }> = ({
   if (
     !note &&
     !entry.transport &&
+    !entry.countsAs &&
+    !entry.armyCap &&
     !entry.rules.length &&
     !entry.extras.length
   ) {
@@ -430,10 +471,12 @@ const Details: React.FC<{ entry: Entry; showNote: boolean }> = ({
   return (
     <div className="flex flex-col gap-1 text-sm">
       {entry.transport && <p>{entry.transport}</p>}
+      {entry.countsAs && <p>{entry.countsAs}</p>}
       {note && <p>{note}</p>}
       {entry.rules.map((text, index) => (
         <p key={index}>{text}</p>
       ))}
+      {entry.armyCap && <p>{entry.armyCap}</p>}
       {entry.extras.map((text, index) => (
         <p key={index}>{text}</p>
       ))}
@@ -508,6 +551,12 @@ export default async function Page(props: {
       max: section.max_percent,
     }));
 
+    const unitByEntryId = new Map(
+      list.unit_categories.flatMap(({ army_list_entries }) =>
+        army_list_entries.map((entry) => [entry.id, entry.units.name] as const),
+      ),
+    );
+
     const categories = list.unit_categories.map((section) => ({
       category: section.category,
       limit: compositionLimit({
@@ -520,7 +569,7 @@ export default async function Page(props: {
         .map(allowanceRule)
         .filter((text): text is string => text !== null),
       entries: section.army_list_entries.map((entry) =>
-        buildEntry(entry, section.category, faction.slug),
+        buildEntry(entry, section.category, faction.slug, unitByEntryId),
       ),
     }));
 
diff --git a/components/CharacteristicProfile.tsx b/components/CharacteristicProfile.tsx
index 6d80198..7044f90 100644
--- a/components/CharacteristicProfile.tsx
+++ b/components/CharacteristicProfile.tsx
@@ -22,7 +22,7 @@ export type Characteristics = {
 export type CharacteristicRow = Characteristics & {
   id: number | string;
   name: string;
-  alternative?: number;
+  alternative?: number | null;
   count?: string | null;
   note?: string | null;
   cost?: string | null;
@@ -30,6 +30,35 @@ export type CharacteristicRow = Characteristics & {
 
 const characteristic = (value: string | null): string => value ?? DASH;
 
+type Alternative = number | null | undefined;
+
+const outsideChoice = (alternative: Alternative): boolean =>
+  alternative === null || alternative === undefined;
+
+// The branch ids a set of rows actually offers a choice between. A lone 0 is
+// not a choice: before the column became nullable every row of a parent with
+// nothing to choose sat at 0, and afterwards it sits at null. Both read the
+// same way here, so the renderer is correct either side of that migration.
+export const branchesOf = (
+  rows: { alternative?: Alternative }[],
+): Set<number> => {
+  const branches = new Set<number>();
+
+  for (const row of rows) {
+    if (!outsideChoice(row.alternative)) {
+      branches.add(row.alternative as number);
+    }
+  }
+
+  return branches.size === 1 && branches.has(0) ? new Set<number>() : branches;
+};
+
+export const inBranch = (
+  row: { alternative?: Alternative },
+  branches: Set<number>,
+): boolean =>
+  !outsideChoice(row.alternative) && branches.has(row.alternative as number);
+
 const HEAD_CELL = "py-1 font-subtitle text-xs text-white";
 const CELL = "py-1";
 
@@ -51,6 +80,7 @@ export const CharacteristicTable: React.FC<{
   named = true,
 }): React.JSX.Element => {
   const priced = rows.some((row) => row.cost);
+  const branches = branchesOf(rows);
 
   return (
     <div className="overflow-x-auto">
@@ -88,9 +118,14 @@ export const CharacteristicTable: React.FC<{
         <tbody className="font-semibold text-base md:text-lg">
           {rows.map((row, index) => {
             const orAbove =
-              index > 0 && row.alternative !== rows[index - 1].alternative;
+              index > 0 &&
+              inBranch(row, branches) &&
+              inBranch(rows[index - 1], branches) &&
+              row.alternative !== rows[index - 1].alternative;
             const orBelow =
               index < rows.length - 1 &&
+              inBranch(row, branches) &&
+              inBranch(rows[index + 1], branches) &&
               rows[index + 1].alternative !== row.alternative;
 
             return (
diff --git a/components/UnitEquipment.tsx b/components/UnitEquipment.tsx
index ebabf73..c418915 100644
--- a/components/UnitEquipment.tsx
+++ b/components/UnitEquipment.tsx
@@ -2,6 +2,8 @@ import {
   LabelledGroup,
   LabelledRow,
   LabelledTable,
+  branchesOf,
+  inBranch,
 } from "@/components/CharacteristicProfile";
 import { generateAnchorId, ruleHref } from "@/lib/anchors";
 import { clsx } from "clsx";
@@ -24,7 +26,7 @@ export const ruleName = (name: string): string => name.split(" (")[0];
 type ProfileWeapon = {
   id: number;
   quantity: number;
-  alternative: number;
+  alternative: number | null;
   position: number;
   weapons: { name: string };
 };
@@ -32,7 +34,7 @@ type ProfileWeapon = {
 type ProfileArmour = {
   armour_id: number;
   position: number;
-  alternative: number;
+  alternative: number | null;
   save_override: string | null;
   armour: { name: string };
 };
@@ -51,6 +53,8 @@ export type EquipmentProfile = {
   unit_profile_wargear_cards: ProfileWargearCard[];
 };
 
+export type WeaponProfileName = { id: number; name: string | null };
+
 export type EquipmentOption = {
   id: number;
   option_group: string;
@@ -62,10 +66,12 @@ export type EquipmentOption = {
   grant_mode: string | null;
   restriction: string | null;
   note: string | null;
+  weapon_profile_id: number | null;
   profile: { name: string } | null;
   upgrade: { name: string; units: { name: string } } | null;
   replaces: { name: string } | null;
-  grants: { name: string } | null;
+  granted: { name: string } | null;
+  grants: { name: string; weapon_profiles: WeaponProfileName[] } | null;
   grants_armour: { name: string } | null;
   replaces_armour: { name: string } | null;
   card: { name: string } | null;
@@ -75,6 +81,15 @@ export type EquipmentOption = {
   }[];
 };
 
+const weaponProfileName = (
+  option: Pick<EquipmentOption, "weapon_profile_id" | "grants">,
+): string | null =>
+  option.weapon_profile_id === null
+    ? null
+    : (option.grants?.weapon_profiles.find(
+        ({ id }) => id === option.weapon_profile_id,
+      )?.name ?? null);
+
 export type SpecialRule = {
   id: number;
   name: string;
@@ -91,6 +106,7 @@ export type SpecialRule = {
 export type SpecialRuleAssignment = {
   position: number;
   note: string | null;
+  unit_profile_id: number | null;
   rule: SpecialRule | null;
 };
 
@@ -101,48 +117,82 @@ export type EquipmentUnit = {
   unit_special_rule_assignments: SpecialRuleAssignment[];
 };
 
+// Armour worn only on one branch. 0 has always meant "worn whichever branch is
+// taken" in this table rather than branch 0, and null means it after the
+// migration, so both read as universal here.
+const isScopedArmour = (piece: ProfileArmour): boolean =>
+  piece.alternative !== null &&
+  piece.alternative !== undefined &&
+  piece.alternative !== 0;
+
+const WeaponLink: React.FC<{ weapon: ProfileWeapon }> = ({
+  weapon,
+}): React.JSX.Element => (
+  <>
+    {weapon.quantity > 1 && `${weapon.quantity} ${TIMES} `}
+    <Link
+      className={LINK}
+      href={`/wargear/weapons#${generateAnchorId(weapon.weapons.name)}`}
+    >
+      {weapon.weapons.name}
+    </Link>
+  </>
+);
+
 const Loadout: React.FC<{
   weapons: ProfileWeapon[];
   armour?: ProfileArmour[];
 }> = ({ weapons, armour = [] }): React.JSX.Element => {
-  const alternatives = new Map<number, ProfileWeapon[]>();
+  const branches = branchesOf(weapons);
+  const always = weapons.filter((weapon) => !inBranch(weapon, branches));
+  const byBranch = new Map<number, ProfileWeapon[]>();
 
   for (const weapon of weapons) {
-    const bucket = alternatives.get(weapon.alternative) ?? [];
+    if (!inBranch(weapon, branches)) {
+      continue;
+    }
+
+    const bucket = byBranch.get(weapon.alternative as number) ?? [];
     bucket.push(weapon);
-    alternatives.set(weapon.alternative, bucket);
+    byBranch.set(weapon.alternative as number, bucket);
   }
 
-  const armourByAlternative = new Map<number, ProfileArmour[]>();
+  const armourByBranch = new Map<number, ProfileArmour[]>();
 
   for (const piece of armour) {
-    const bucket = armourByAlternative.get(piece.alternative) ?? [];
+    if (!inBranch(piece, branches)) {
+      continue;
+    }
+
+    const bucket = armourByBranch.get(piece.alternative as number) ?? [];
     bucket.push(piece);
-    armourByAlternative.set(piece.alternative, bucket);
+    armourByBranch.set(piece.alternative as number, bucket);
   }
 
-  const loadouts = [...alternatives.entries()].sort(([a], [b]) => a - b);
+  const loadouts = [...byBranch.entries()].sort(([a], [b]) => a - b);
 
   return (
     <>
-      {loadouts.map(([alternative, group], loadout) => (
-        <Fragment key={alternative}>
-          {loadout > 0 && (
+      {always.map((weapon, index) => (
+        <Fragment key={weapon.id}>
+          {index > 0 && ", "}
+          <WeaponLink weapon={weapon} />
+        </Fragment>
+      ))}
+      {loadouts.map(([branch, group], loadout) => (
+        <Fragment key={branch}>
+          {loadout > 0 ? (
             <span className="font-subtitle">{" \u2014or\u2014 "}</span>
+          ) : (
+            Boolean(always.length) && ", "
           )}
           {group.map((weapon, index) => (
             <Fragment key={weapon.id}>
               {index > 0 && ", "}
-              {weapon.quantity > 1 && `${weapon.quantity} ${TIMES} `}
-              <Link
-                className={LINK}
-                href={`/wargear/weapons#${generateAnchorId(weapon.weapons.name)}`}
-              >
-                {weapon.weapons.name}
-              </Link>
+              <WeaponLink weapon={weapon} />
             </Fragment>
           ))}
-          {(armourByAlternative.get(alternative) ?? []).map((piece) => (
+          {(armourByBranch.get(branch) ?? []).map((piece) => (
             <Fragment key={`armour-${piece.armour_id}`}>
               {", "}
               <Link
@@ -169,7 +219,7 @@ const loadoutKey = (profile: {
       .map((w) => `${w.alternative}:${w.quantity}:${w.weapons.name}`)
       .join("|"),
     profile.unit_profile_armour
-      .filter((piece) => piece.alternative !== 0)
+      .filter(isScopedArmour)
       .map(
         (piece) =>
           `${piece.alternative}:${piece.armour.name}:${piece.save_override ?? ""}`,
@@ -263,6 +313,9 @@ export const UnitEquipment: React.FC<{
               key: `Rule-${assignment.rule.id}`,
               note: assignment.note,
               rule: assignment.rule,
+              profile:
+                profiles.find(({ id }) => id === assignment.unit_profile_id) ??
+                null,
             },
           ]
         : [],
@@ -275,9 +328,9 @@ export const UnitEquipment: React.FC<{
     (profile) => profile.unit_profile_weapons.length,
   );
   const universalArmour = (profile: EquipmentProfile): ProfileArmour[] =>
-    profile.unit_profile_armour.filter((piece) => piece.alternative === 0);
+    profile.unit_profile_armour.filter((piece) => !isScopedArmour(piece));
   const scopedArmour = (profile: EquipmentProfile): ProfileArmour[] =>
-    profile.unit_profile_armour.filter((piece) => piece.alternative !== 0);
+    profile.unit_profile_armour.filter(isScopedArmour);
   const armoured = profiles.filter(
     (profile) => universalArmour(profile).length,
   );
@@ -484,6 +537,12 @@ export const UnitEquipment: React.FC<{
                         compact={compact}
                       >
                         <strong className={blockProse ? "block" : undefined}>
+                          {named && row.profile && (
+                            <>
+                              {row.profile.name}
+                              {` ${DASH} `}
+                            </>
+                          )}
                           {href ? (
                             <Link className={LINK} href={href}>
                               {display}
@@ -585,11 +644,13 @@ export const UnitEquipment: React.FC<{
                     ? (["weapon", "weapons"] as const)
                     : (["item", "equipment"] as const);
                   const cost = optionCosts?.get(option.id) ?? null;
+                  const profileName = weaponProfileName(option);
                   const structured = Boolean(
                     option.profile ||
                     scope ||
                     sections.length ||
                     option.upgrade ||
+                    option.granted ||
                     option.grants ||
                     option.grants_armour ||
                     option.card ||
@@ -616,6 +677,7 @@ export const UnitEquipment: React.FC<{
                         Boolean(
                           sections.length ||
                           option.upgrade ||
+                          option.granted ||
                           option.grants ||
                           option.grants_armour ||
                           option.card ||
@@ -634,6 +696,14 @@ export const UnitEquipment: React.FC<{
                           </Link>
                         </>
                       )}
+                      {option.granted && (
+                        <Link
+                          className={LINK}
+                          href={`#${entryAnchor(option.granted.name)}`}
+                        >
+                          {option.granted.name}
+                        </Link>
+                      )}
                       {option.grants && (
                         <>
                           {!sections.length &&
@@ -649,6 +719,7 @@ export const UnitEquipment: React.FC<{
                           >
                             {option.grants.name}
                           </Link>
+                          {profileName && ` (${profileName})`}
                         </>
                       )}
                       {option.grants_armour && (
